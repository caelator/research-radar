Reading additional input from stdin...
OpenAI Codex v0.118.0 (research preview)
--------
workdir: /Users/bri/Documents/GitHub/research-radar
model: gpt-5.4
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /Users/bri/Documents/GitHub/research-radar, /Users/bri/.codex/memories]
reasoning effort: none
reasoning summaries: none
session id: 019d618b-054f-7711-beef-bb011d9a74a3
--------
user
You are the deliberation lead (synthesis-owner) in an AI planning council.

Your job:
- Review EVERY critique point from adversarial reviewers — do not skip any
- Classify each as ACCEPT / PARTIAL / REJECT with a one-sentence rationale
- Produce a revised plan incorporating accepted/partial changes
- Keep decisions concise to avoid truncation
- Preserve strengths called out by reviewers unless a change requires otherwise

Telemetry-guided working method:
- Triage critiques in this order: LOW first, then MEDIUM, then HIGH. This prevents
  defensive overreaction and usually surfaces easy wins early.
- For every ACCEPT or PARTIAL, identify the exact plan section to modify and the
  concrete delta you will apply before writing the revised plan.
- For PARTIAL, accept the valid core of the critique and explicitly scope the fix
  to the smallest change that resolves the real issue.

Severity-proportional disposition (IMPORTANT — follow strictly):
- **Low-severity critiques → almost always ACCEPT.** These are minor improvements
  by definition. Accept them unless the suggested change is actively harmful.
  Rejecting a low-severity critique requires strong justification.
- **Medium-severity critiques → lean ACCEPT or PARTIAL.** Default to ACCEPT when
  the critique identifies a real gap. Use PARTIAL when directionally correct but
  the suggested fix is too broad — accept the spirit, scope down the change.
- **High-severity critiques → evaluate carefully.** These claim the plan would fail.
  If the claim is valid, ACCEPT. If overstated, PARTIAL with a scoped fix.
  REJECT only if the issue genuinely doesn't exist.
- REJECT is reserved for critiques that misunderstand the plan, identify a
  non-existent problem, or propose a fix that makes the design worse.
  Every REJECT must include a specific explanation — not just a dismissal.

Zero-acceptance safeguard:
- If you are about to REJECT every critique, STOP. Re-read each one and find at
  least one genuine improvement to ACCEPT. A 100% rejection rate almost always
  means you are being too defensive, not that every critique is wrong.
- If you find yourself rejecting more than a third of critiques, pause and
  reconsider whether you are defending the plan rather than improving it.

Current plan:
Here is the revised plan, synthesizing the original goals with the elegance and feasibility contributions. Sections that have been updated are marked with `[CHANGED]`.

## 1. Goal and constraints
- **Goal**: Design a unified, shared data layer embedded within the `layers` codebase to serve as the single source of truth for all Caelator ecosystem plugins (`council`, `openclaw-pm`, `research-radar`, `evolve`).
- **Constraint**: Must strictly use LanceDB as the underlying vector and structured data store for shared, queryable state. No alternative databases may be considered for this purpose.
- **Constraint**: The data layer must support both semantic (vector) search and structured (relational/metadata) queries natively.
- **Constraint**: Plugins must cease maintaining isolated persistent storage silos for shared state; the design must prevent data duplication across the ecosystem.
- **Constraint**: This phase is strictly for architectural design and documentation; no Rust implementation or plugin refactoring is to be executed yet.

## 2. Candidate approaches [CHANGED]

### Approach A: Single Monolithic Collection (The "Everything Bagel")
- **Concept**: A single LanceDB collection (`caelator_memory`) stores every entity across all plugins. It uses a flexible schema with a generic `vector` column, standard metadata (`id`, `plugin_source`, `timestamp`, `type`), and a flexible JSON/Blob column for plugin-specific payload data.
- **Tradeoffs**:
  - *Pros*: Trivial cross-plugin semantic search (one query searches everything). Minimal collection management overhead.
  - *Cons*: Highly sparse data structures. Loss of strong typing at the database level (requires heavy serialization/deserialization logic in Rust). 

### Approach B: Domain-Specific Collections with a Unified Relational Graph
- **Concept**: Define a fixed set of core, strongly-typed domain collections in LanceDB. All collections share a mandatory "Unified Memory Header" (standardized fields like `urn`, `parent_urn`, `tags`) to allow cross-referencing and ecosystem-wide filtering. Embeddings are stored inline.
- **Tradeoffs**:
  - *Pros*: Preserves strong Rust typing. Cleaner schema evolution per domain.
  - *Cons*: Cross-domain semantic searches require multi-collection queries and aggregation in the application logic. Inline embeddings complicate model migrations.

### Approach C: Hybrid (LanceDB for Shared State/Vectors + File-based Append Logs)
- **Concept**: Use LanceDB with strongly-typed canonical collections (`entities`, `relations`, `artifacts`, `projections`) and a dedicated, separate `embeddings` collection keyed by `(subject_urn, embedding_space, chunk_id)`. Simultaneously, exempt pure temporal audit streams (e.g., `council`'s JSONL traces, `openclaw-pm`'s event logs) from LanceDB, retaining them as file-based append-only logs.
- **Tradeoffs**:
  - *Pros*: Avoids forcing non-semantic, append-only audit data into a vector DB, reducing schema complexity by ~40%. A dedicated `embeddings` table gives one uniform retrieval surface while preserving domain typing in other collections. 
  - *Cons*: Requires defining clear boundaries between what is "shared state" (LanceDB) and what is "local audit log" (Filesystem).

## 3. Recommended approach with rationale [CHANGED]
**Recommended: Approach C (Hybrid with Dedicated Embedding Index and File-based Logs)**
- **Rationale**: Rust thrives on strong typing, and LanceDB's Arrow-backed storage is most performant when schemas are well-defined. However, forcing append-only operational logs (like `RunTrace` or `events.jsonl`) into LanceDB adds unnecessary complexity. The Hybrid approach isolates shared, queryable state into LanceDB while leaving temporal streams as simple files.
- **Dedicated Embeddings Table**: Instead of storing vectors directly in every domain collection, semantic search will flow through a central `embeddings` table. This makes multi-collection retrieval, model migration, and re-embedding much more efficient.
- **Greenfield Vector Spaces**: Since `research-radar` currently uses SQLite with keyword `LIKE` matching and does not actually have an embedding infrastructure yet, we are introducing vector spaces for the first time. We can dictate a single embedding model/space standard from day one, avoiding cross-model compatibility issues entirely.
- **Unified Memory Language**: By enforcing a standard `MemoryHeader` struct on every domain collection, plugins can still perform ecosystem-wide operations via relational queries.

## 4. V1 scope [CHANGED]
- **Deliverable**: A comprehensive markdown Architecture Design Document (ADD).
- **Schema Definition**: Definition of the "Unified Memory Language". Instead of abstract top-down names, the schema will map actual existing domain types (e.g., `sources`, `entries`, `profiles`, `project_state`, `tasks`) into a stable, canonical set of tables: `entities`, `relations`, `artifacts`, `embeddings`, and `projections`. The ADD must define each table’s primary key, required columns, reference fields, and ownership rules.
- **API Surface Design**: Instead of a generic `DataLayer` trait with runtime broker bindings, the design will define a typed Rust crate (`layers-data`) that plugins depend on directly as a library. This crate will expose capability-specific interfaces (`EntityStore`, `RelationStore`, `SemanticIndex`, `EventLog`, `QueryPlanner`).
- **Migration Strategy**: A mechanical, concrete ETL mapping matrix for all plugins. For example, explicitly defining how `research-radar`'s 9 SQLite tables migrate (`sources` → `rr_sources` table, `entries` → `rr_entries` with backfilled embeddings, dropping/deferring ephemeral tables like `scan_jobs`).

## 5. V2 / later
- **Implementation**: Writing the actual Rust code in the `layers` codebase to instantiate the LanceDB connection and expose the APIs.
- **Plugin Refactoring**: Updating `research-radar`, `council`, and `openclaw-pm` source code to consume the new `layers` API and deleting their isolated local storage mechanisms.
- **Advanced Graph Queries**: Implementing multi-hop relational queries across LanceDB collections within the `layers` API layer.
- **Data Sync**: Mechanisms for synchronizing local LanceDB storage across different developer machines or to a cloud backup.

## 6. Out of scope / do not build
- Evaluation, benchmarking, or consideration of any database other than LanceDB (e.g., SQLite, Qdrant, Chroma).
- Writing any Rust implementation code for the data layer or modifying existing plugin codebases during this design phase.
- Design of network-level synchronization protocols or multi-user concurrent access models (assume single-user local dev environment for now).
- Designing generic data ingestors for non-Caelator external tools.

## 7. Files, binaries, and storage [CHANGED]
- `docs/architecture/001-unified-data-layer.md`: The canonical output of this phase, detailing the schema, API, and migration plan.
- **Storage Paths**: The design will explicitly accommodate both user-global and workspace-relative storage. 
  - Global data (formerly `~/.research-radar/data.db`) will reside at `~/.caelator/store/*.lance`.
  - Project-scoped data (formerly `.openclaw/pm/`) will reside in a workspace-relative LanceDB instance (e.g., `<workspace>/.caelator/store/*.lance`) or utilize a strict workspace-keying scheme within the global store. 
- `crates/layers-data/src/`: The directory within the `layers` codebase where the unified schema definitions, capability traits, and LanceDB library implementations will reside in V2.

## 8. Control flow [CHANGED]
1. **Initialization**: The Caelator runtime starts. Plugins use direct compile-time crate dependencies to call `layers_data::open(path)`, returning typed capability handles (`EntityStore`, `SemanticIndex`, etc.).
2. **Schema Verification**: `layers_data` ensures all required canonical collections exist and are up-to-date with the current schema.
3. **Identity & Namespace**: Every plugin must establish a scoped `PluginContext { plugin_id, workspace_id, capabilities }`. 
4. **Write Path**: `research-radar` indexes a new file. It constructs an entity implementing the Unified Memory trait, attaching a globally unique `urn`, `workspace_id`, and `origin_plugin`. It calls `entity_store.insert(doc)`.
5. **Read Path**: `council` needs context. It calls `semantic_index.search(query_vector)`. The index layer resolves the `embeddings` table matches back to their canonical domain records and returns normalized `MemoryNode` structs.

## 9. Risks and open questions [CHANGED]
- **LanceDB Rust API Maturity**: LanceDB's Rust SDK is evolving rapidly. Does it currently support all the complex filtering and cross-collection operations we require without dropping down to raw DataFusion queries?
- **Concurrency and File Locking**: `openclaw-pm` relies heavily on `fs2` exclusive file locks and atomic temp-file-rename writes for state mutations. LanceDB uses an MVCC concurrency model. The design must validate whether LanceDB's concurrency guarantees can safely replace `openclaw-pm`'s strict locking patterns without silently degrading atomicity.
- **Schema Evolution**: How do we handle database migrations when the Unified Memory schema needs to change, given that multiple plugins depend on it simultaneously?
- **Embedding Model Alignment**: To ensure a unified semantic store, the architecture must treat embedding provenance as a strict schema constraint. We must define approved embedding spaces up front, require vectors to declare `space_id`, and disallow cross-space semantic searches.

## 10. Validation plan [CHANGED]
- **Schema Desk-Check**: Map existing data structures from all plugins to the proposed canonical LanceDB schema to ensure no data loss or awkward serialization is required.
- **Migration Mapping Matrix**: Produce a full migration mapping matrix for `research-radar`, `openclaw-pm`, `council`, and `evolve`. This must show the current persisted entity, the target unified schema rows, and the deprecated local store path, with at least one worked end-to-end example per plugin.
- **API Walkthrough**: Write pseudocode for the top 3 most common plugin operations using the proposed `layers-data` capability interfaces to verify developer ergonomics.


Critiques:
## Claude (conceptual-risk)
{
  "reviewer_role": "adversarial-reviewer/conceptual-risk",
  "critiques": [
    {
      "issue": "Schema evolution strategy is listed as an open question but left unresolved, despite being load-bearing for the entire multi-plugin compile-time coupling design.",
      "severity": "high",
      "why_it_matters": "Every plugin will take a compile-time dependency on `layers-data`. A breaking schema change (adding a required column, changing a URN format, altering the MemoryHeader) forces lockstep releases of all plugins simultaneously. Without a concrete versioning or migration strategy in the ADD itself, the first schema change post-V2 will either break plugins or force ad-hoc compatibility hacks that undermine the 'single source of truth' goal.",
      "suggested_delta": "Section 9 should be promoted into the V1 deliverable: the ADD must define a schema versioning mechanism (e.g., monotonic schema version per collection, forward-compatible column addition policy, a `layers_data::migrate()` entrypoint) and state whether plugins pin to a schema version range or must always track HEAD.",
      "evidence": "Section 9: 'How do we handle database migrations when the Unified Memory schema needs to change, given that multiple plugins depend on it simultaneously?' — listed as open question, not addressed in V1 scope or validation plan."
    },
    {
      "issue": "The boundary between 'shared state' (LanceDB) and 'local audit log' (filesystem) — the defining characteristic of Approach C — is never actually specified.",
      "severity": "medium",
      "why_it_matters": "The Hybrid approach was chosen specifically because it separates shared queryable state from append-only logs. But the ADD deliverable (Section 4) only defines the LanceDB side. Without explicit criteria for what qualifies as an audit log vs. shared state, each plugin team will draw the line differently, recreating the silo problem the design aims to solve.",
      "suggested_delta": "Add a concrete classification rule to the V1 ADD, e.g.: 'Data is filesystem-local if and only if it is (a) append-only, (b) never queried cross-plugin, and (c) not semantically searchable. All other persisted state must reside in LanceDB.' Then apply this rule to every existing persistence artifact in the migration matrix.",
      "evidence": "Section 3: 'The Hybrid approach isolates shared, queryable state into LanceDB while leaving temporal streams as simple files.' Section 2C cons: 'Requires defining clear boundaries between what is shared state and what is local audit log.'"
    },
    {
      "issue": "The `projections` canonical table is named four times but never defined — its schema, purpose, producers, and consumers are absent.",
      "severity": "medium",
      "why_it_matters": "The migration matrix and API walkthrough validation steps cannot be completed if a core table lacks a definition. Plugin authors reading the ADD will not know whether their derived/computed data belongs in `projections` or `entities`, leading to inconsistent usage from day one.",
      "suggested_delta": "In Section 4's schema definition, add a definition block for `projections` with the same specificity as the other tables (primary key, required columns, ownership rules), or explicitly defer it to V2 and remove it from the canonical table list to avoid confusion.",
      "evidence": "Section 4: '…a stable, canonical set of tables: entities, relations, artifacts, embeddings, and projections.' No further mention of projections' schema or role."
    },
    {
      "issue": "The URN scheme is referenced as the primary cross-collection identity mechanism but is never specified.",
      "severity": "low",
      "why_it_matters": "URNs appear in the MemoryHeader (`urn`, `parent_urn`), the embeddings table key (`subject_urn`), and the relational graph. Without a defined syntax (namespace hierarchy, allowed characters, uniqueness scope), two plugins could independently generate colliding or unparseable URNs.",
      "suggested_delta": "The V1 ADD should include a URN specification section: format (e.g., `urn:caelator:{plugin}:{type}:{id}`), uniqueness guarantees, and whether URNs are opaque handles or parseable identifiers.",
      "evidence": "Section 8: 'attaching a globally unique urn, workspace_id, and origin_plugin' — URN is used as a primary key but its format is undefined."
    },
    {
      "issue": "The task description states LanceDB is 'already used by research-radar for embeddings' but the plan correctly notes research-radar uses SQLite with LIKE matching and has no embedding infrastructure.",
      "severity": "low",
      "why_it_matters": "If stakeholders read the task framing and assume existing LanceDB integration exists in research-radar, they may underestimate migration effort or skip embedding model selection from the V1 scope.",
      "suggested_delta": "Add an explicit note in the ADD's migration matrix that research-radar's LanceDB migration is greenfield (no existing embeddings to port), and that embedding model selection is a prerequisite blocking the migration, not an implementation detail.",
      "evidence": "Section 3: 'research-radar currently uses SQLite with keyword LIKE matching and does not actually have an embedding infrastructure yet.'"
    },
    {
      "issue": "The `QueryPlanner` capability interface is listed alongside domain-specific stores but its responsibilities overlap ambiguously with the other interfaces.",
      "severity": "low",
      "why_it_matters": "If `EntityStore` already supports filtered queries and `SemanticIndex` handles vector search, it is unclear what `QueryPlanner` adds. Plugin authors may bypass it or duplicate query logic, creating inconsistent access patterns.",
      "suggested_delta": "Either define QueryPlanner's unique responsibility (e.g., cross-collection joins, query optimization, hybrid structured+vector queries) or remove it from the V1 API surface and defer to V2 when real usage patterns emerge.",
      "evidence": "Section 4: 'EntityStore, RelationStore, SemanticIndex, EventLog, QueryPlanner' — QueryPlanner is the only interface without a corresponding canonical table or clear domain."
    }
  ],
  "things_to_keep": [
    "Dedicated embeddings table decoupled from domain collections — this is the single best architectural decision in the plan, enabling model migration and multi-domain semantic search without schema churn in every collection.",
    "Hybrid approach exempting append-only audit logs from LanceDB — correctly avoids forcing non-semantic temporal data into a vector store, reducing schema complexity.",
    "Compile-time crate dependency model over runtime broker/RPC — idiomatic Rust, catches integration errors at compile time, avoids the complexity of a service mesh for a single-user local tool.",
    "Migration mapping matrix as a V1 validation artifact — forces concrete confrontation with real data shapes rather than hand-waving about 'just move it over'.",
    "Greenfield vector space recognition for research-radar — correctly identifies the opportunity to set embedding standards from scratch rather than inheriting technical debt."
  ],
  "schema_version": 1
}

## Gemini (implementation-risk)
{
  "reviewer_role": "adversarial-reviewer/implementation-risk",
  "critiques": [
    {
      "issue": "Lack of a concrete concurrency management strategy for LanceDB writes.",
      "severity": "high",
      "why_it_matters": "LanceDB's MVCC supports concurrent reads, but concurrent writes to the same dataset from multiple processes (plugins) can lead to transaction conflicts or data corruption. If the architecture does not mediate writes, the shared data layer will fail under concurrent ecosystem load.",
      "suggested_delta": "Update the V1 Scope and API Surface Design to mandate a centralized write-mediation pattern in `layers-data` (e.g., a single writer actor/daemon, or a strict cross-process file-locking wrapper) rather than just leaving concurrency as an 'open question'.",
      "evidence": "Section 9: 'The design must validate whether LanceDB's concurrency guarantees can safely replace openclaw-pm's strict locking patterns...'"
    },
    {
      "issue": "No mechanism specified to prevent vector drift in the dedicated embeddings table.",
      "severity": "medium",
      "why_it_matters": "Because embeddings are stored in a separate table from the domain entities, deleting or updating a source entity without native database triggers will leave orphaned or stale vectors, degrading semantic search accuracy over time.",
      "suggested_delta": "Require the `layers-data` API to implement application-level cascading deletes and upserts. The EntityStore must automatically synchronize changes to the SemanticIndex table within the same API call.",
      "evidence": "Section 3: 'semantic search will flow through a central `embeddings` table. This makes multi-collection retrieval... much more efficient.'"
    },
    {
      "issue": "Relational integrity boundary between LanceDB and file-based logs is undefined.",
      "severity": "medium",
      "why_it_matters": "By keeping audit streams in files while entities live in LanceDB, entities might be deleted or mutated while file logs still reference them. This breaks the ability to reconstruct accurate historical traces.",
      "suggested_delta": "Explicitly define a lifecycle policy in the Architecture Design Document for cross-boundary references, such as mandating 'soft deletes' (tombstoning) in LanceDB to ensure file-based logs never point to missing URNs.",
      "evidence": "Section 2, Approach C: 'exempt pure temporal audit streams... retaining them as file-based append-only logs.'"
    },
    {
      "issue": "Indecision on workspace isolation architecture.",
      "severity": "low",
      "why_it_matters": "Failing to commit to either a single global store with workspace keys OR multiple workspace-relative stores leaves a massive API design hole that will block V2 implementation.",
      "suggested_delta": "Mandate that the V1 Architecture Design Document must make a definitive decision between the two isolation models rather than accommodating both loosely.",
      "evidence": "Section 7: 'reside in a workspace-relative LanceDB instance... or utilize a strict workspace-keying scheme within the global store.'"
    }
  ],
  "things_to_keep": [
    "The decision to separate the embeddings table from domain tables (Approach C) is an excellent choice for model migration and unified semantic search.",
    "The strict requirement for a migration mapping matrix is highly pragmatic and reduces the risk of data loss.",
    "Introducing vector spaces explicitly as a schema constraint."
  ],
  "schema_version": 1
}

Task:
Design a data layer implementation. The data layer is a shared storage and semantic retrieval substrate for the Caelator agent ecosystem. Requirements: (1) LanceDB as the backing vector/structured store, already used by research-radar for embeddings; (2) a unified memory schema — a shared data "language" that all plugins (layers, council, openclaw-pm, research-radar, evolve) speak and query instead of maintaining their own isolated stores; (3) the data layer is built into the layers codebase as the canonical shared substrate; (4) plugins stop duplicating storage and instead query this common semantic + structured store. The question: what is the best architecture, schema design, API surface, and integration pattern to make this real? Focus on: schema design for the unified memory language, LanceDB table/collection design, how layers exposes the data layer to plugins, how plugin-to-layer binding works, and what the migration path looks like for existing plugins that currently have their own storage.

Return EXACTLY two sections in this format:

## Decision Log
Group decisions by source reviewer using ### subheadings.
Use this EXACT format (one line per decision):

### Claude (conceptual-risk)
- ACCEPT: <issue summary> -- <rationale>
- PARTIAL: <issue summary> -- <rationale>
- REJECT: <issue summary> -- <rationale>

### Gemini (implementation-risk)
- ACCEPT: <issue summary> -- <rationale>
- PARTIAL: <issue summary> -- <rationale>
- REJECT: <issue summary> -- <rationale>

You MUST address every critique point listed above. Do not skip any.
Keep rationales to one sentence each.

## Revised Plan
The updated plan with accepted changes incorporated. Mark changed sections
with [CHANGED] tags. Preserve all original sections that were not changed.

Keep the revised plan CONCISE — do not expand unchanged sections. Only rewrite
sections that were actually modified by accepted/partial critiques. Target the
same length as the input plan or shorter.
For each [CHANGED] section, make the delta obvious and directly traceable to one
or more ACCEPT/PARTIAL decisions above.
2026-04-06T06:46:34.984024Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer realm=\"OAuth\", resource_metadata=\"https://mcp.cloudflare.com/.well-known/oauth-protected-resource/mcp\", error=\"invalid_token\", error_description=\"Missing or invalid access token\"" })
2026-04-06T06:46:35.094737Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer error=\"invalid_request\", error_description=\"No access token was provided in this request\", resource_metadata=\"https://mcp.supabase.com/.well-known/oauth-protected-resource/mcp\"" })
codex
## Decision Log
### Claude (conceptual-risk)
- ACCEPT: Define schema evolution/versioning in V1 rather than leaving it as an open question -- compile-time coupling makes compatibility policy load-bearing, so the ADD must specify versioning, migration entrypoints, and plugin compatibility rules.
- ACCEPT: Specify the boundary between shared state and filesystem audit logs -- the hybrid architecture only works if the ADD gives a deterministic classification rule and applies it in the migration matrix.
- ACCEPT: Define `projections` explicitly in the canonical schema -- keeping it undefined would make the migration matrix and table ownership rules incomplete.
- ACCEPT: Specify the URN format and uniqueness scope -- URNs are the cross-table identity backbone, so leaving them implicit invites collisions and inconsistent parsing.
- ACCEPT: Call out `research-radar` migration as greenfield for embeddings and make model selection a prerequisite -- that clarification prevents stakeholders from underestimating migration work.
- ACCEPT: Define `QueryPlanner` narrowly instead of leaving it ambiguous -- it should exist only for hybrid structured+semantic queries and cross-collection resolution, not as a duplicate of store APIs.

### Gemini (implementation-risk)
- PARTIAL: Mandate write mediation for LanceDB concurrency -- the ADD should require a single write-coordination mechanism in `layers-data`, but V1 only needs the design contract and validation criteria, not a daemon implementation.
- ACCEPT: Prevent vector drift between entities and the dedicated embeddings table -- the API must make entity/embedding lifecycle synchronization an invariant via coordinated upserts and tombstoning.
- ACCEPT: Define cross-boundary integrity for LanceDB entities referenced by file logs -- soft deletes/tombstones are needed so append-only logs never point at vanished URNs.
- ACCEPT: Choose one workspace isolation model instead of leaving both open -- the architecture should commit to a single canonical store model so V2 APIs have a stable target.

## Revised Plan
## 1. Goal and constraints
- **Goal**: Design a unified, shared data layer embedded within the `layers` codebase to serve as the single source of truth for all Caelator ecosystem plugins (`council`, `openclaw-pm`, `research-radar`, `evolve`).
- **Constraint**: Must strictly use LanceDB as the underlying vector and structured data store for shared, queryable state. No alternative databases may be considered for this purpose.
- **Constraint**: The data layer must support both semantic (vector) search and structured (relational/metadata) queries natively.
- **Constraint**: Plugins must cease maintaining isolated persistent storage silos for shared state; the design must prevent data duplication across the ecosystem.
- **Constraint**: This phase is strictly for architectural design and documentation; no Rust implementation or plugin refactoring is to be executed yet.

## 2. Candidate approaches
### Approach A: Single Monolithic Collection (The "Everything Bagel")
- **Concept**: A single LanceDB collection (`caelator_memory`) stores every entity across all plugins with generic metadata plus a flexible payload blob.
- **Tradeoffs**:
  - *Pros*: Trivial cross-plugin semantic search. Minimal collection management overhead.
  - *Cons*: Sparse schema, weak typing, heavy serialization logic.

### Approach B: Domain-Specific Collections with a Unified Relational Graph
- **Concept**: Fixed, strongly-typed domain collections share a mandatory `MemoryHeader` (`urn`, `parent_urn`, `workspace_id`, `tags`, `origin_plugin`), with embeddings stored inline.
- **Tradeoffs**:
  - *Pros*: Strong Rust typing. Cleaner per-domain schema evolution.
  - *Cons*: Multi-collection semantic aggregation required. Inline vectors complicate re-embedding.

### Approach C: Hybrid (LanceDB for Shared State/Vectors + File-based Append Logs)
- **Concept**: Use strongly-typed LanceDB collections (`entities`, `relations`, `artifacts`, `embeddings`, `projections`) plus filesystem append logs for pure audit streams.
- **Tradeoffs**:
  - *Pros*: Keeps shared queryable state centralized while avoiding misuse of LanceDB for temporal traces; dedicated embeddings table preserves domain typing and simplifies re-indexing.
  - *Cons*: Requires an explicit classification rule and cross-boundary lifecycle policy.

## 3. Recommended approach with rationale [CHANGED]
**Recommended: Approach C (Hybrid with Dedicated Embedding Index and File-based Logs)**
- **Rationale**: Keep all shared, queryable, cross-plugin state in LanceDB; keep data filesystem-local **only if** it is append-only, never queried cross-plugin, and not semantically searchable.
- **Dedicated Embeddings Table**: Semantic retrieval flows through `embeddings(subject_urn, space_id, chunk_id, vector, content_hash, status)` so model migration and multi-domain search do not churn domain schemas.
- **Greenfield Vector Spaces**: `research-radar` is migrating from SQLite keyword search, not from an existing embedding store; embedding model/space selection is therefore a V1 prerequisite, not a migration afterthought.
- **Unified Memory Language**: Every canonical row carries a standard `MemoryHeader`, and file logs may reference URNs but never replace canonical state.

## 4. V1 scope [CHANGED]
- **Deliverable**: A comprehensive markdown Architecture Design Document (ADD).
- **Schema Definition**: Define canonical tables and ownership rules:
  - `entities`: canonical stateful nodes keyed by `urn`.
  - `relations`: typed edges between URNs.
  - `artifacts`: larger content blobs or external-file descriptors attached to URNs.
  - `embeddings`: semantic index rows keyed by `(subject_urn, space_id, chunk_id)`.
  - `projections`: derived, rebuildable read models/materializations keyed by `(projection_name, subject_urn, version)`; never the system of record.
- **Identity Spec**: The ADD must define URNs as `urn:caelator:{plugin_or_domain}:{kind}:{id}` with global uniqueness, allowed character rules, and the rule that URNs are opaque for logic but parseable for diagnostics.
- **Schema Evolution**: Promote this into V1: additive columns are the default, each collection has a monotonic schema version, `layers_data::migrate()` upgrades stores in place, and plugins declare a supported schema-version range rather than assuming HEAD.
- **API Surface Design**: Define a typed Rust crate (`layers-data`) exposing `EntityStore`, `RelationStore`, `ArtifactStore`, `SemanticIndex`, `EventLog`, and `QueryPlanner`.
  - `QueryPlanner` is only for cross-collection resolution and hybrid structured+semantic queries; simple CRUD/filtering stays in the store traits.
  - Entity/artifact writes must synchronize embedding upserts/tombstones through the same API boundary.
  - Write concurrency is mediated by one mandated mechanism inside `layers-data` (initial design target: a cross-process write coordinator/lock wrapper so only one writer commits at a time).
- **Migration Strategy**: Include a concrete ETL matrix for every persisted artifact in every plugin, and classify each as `LanceDB canonical` or `filesystem audit` using the rule above.

## 5. V2 / later
- **Implementation**: Writing the actual Rust code in the `layers` codebase to instantiate the LanceDB connection and expose the APIs.
- **Plugin Refactoring**: Updating `research-radar`, `council`, and `openclaw-pm` source code to consume the new `layers` API and deleting their isolated local storage mechanisms.
- **Advanced Graph Queries**: Implementing multi-hop relational queries across LanceDB collections within the `layers` API layer.
- **Data Sync**: Mechanisms for synchronizing local LanceDB storage across different developer machines or to a cloud backup.

## 6. Out of scope / do not build
- Evaluation, benchmarking, or consideration of any database other than LanceDB (e.g., SQLite, Qdrant, Chroma).
- Writing any Rust implementation code for the data layer or modifying existing plugin codebases during this design phase.
- Design of network-level synchronization protocols or multi-user concurrent access models (assume single-user local dev environment for now).
- Designing generic data ingestors for non-Caelator external tools.

## 7. Files, binaries, and storage [CHANGED]
- `docs/architecture/001-unified-data-layer.md`: The canonical output of this phase, detailing schema, API, migration, versioning, and audit-log classification.
- **Canonical Store Decision**: Use one user-global LanceDB store at `~/.caelator/store/*.lance` with mandatory `workspace_id` on every canonical row; workspace-relative storage is reserved for append-only local audit logs only.
- `crates/layers-data/src/`: The directory within the `layers` codebase where schema definitions, capability traits, and LanceDB implementations will live in V2.

## 8. Control flow [CHANGED]
1. **Initialization**: Plugins call `layers_data::open(global_store_path, plugin_context)` and receive typed capability handles.
2. **Schema + Compatibility Check**: `layers-data` verifies collection schema versions, runs `layers_data::migrate()` if needed, and rejects unsupported plugin/schema version combinations.
3. **Identity & Namespace**: Every write includes `PluginContext { plugin_id, workspace_id, capabilities }` and a generated canonical URN.
4. **Write Path**: A plugin writes through `EntityStore`/`ArtifactStore`; `layers-data` acquires the write coordinator, persists canonical rows, and synchronizes embedding rows or tombstones in the same mediated operation.
5. **Read Path**: `QueryPlanner` composes structured filters plus `SemanticIndex` hits, resolves `subject_urn` back to canonical records, and returns normalized `MemoryNode` results.
6. **Audit Path**: Pure append-only traces are written to workspace-local logs and may reference URNs, but canonical records they reference are tombstoned rather than hard-deleted.

## 9. Risks and open questions [CHANGED]
- **LanceDB Rust API Maturity**: Validate that required filtering, joins, and projection rebuild workflows are supportable without excessive raw-engine escape hatches.
- **Write Coordination Feasibility**: Confirm the chosen single-writer mediation pattern is sufficient for expected plugin concurrency and does not make common workflows unusably serial.
- **Schema Evolution Operations**: The versioning policy is defined in V1, but migration ergonomics and rollback strategy still need validation.
- **Embedding Space Governance**: Approved `space_id` values, model provenance fields, and re-embedding triggers must be finalized in the ADD.
- **Projection Discipline**: Ensure projections remain rebuildable caches/read models and never accumulate canonical business state.

## 10. Validation plan [CHANGED]
- **Schema Desk-Check**: Map existing plugin data structures to the canonical schema, including explicit `projections` usage or confirmation that no projection is needed.
- **Migration Mapping Matrix**: For every persisted artifact in `research-radar`, `openclaw-pm`, `council`, and `evolve`, record source store, target canonical rows or audit-log classification, deprecated path, and one worked end-to-end example per plugin.
- **Compatibility Walkthrough**: Include pseudocode for schema-version negotiation, `layers_data::migrate()`, and one non-breaking additive schema change.
- **Integrity Walkthrough**: Include pseudocode for coordinated entity+embedding upsert/delete, tombstoning policy for URNs referenced by file logs, and orphan-vector prevention checks.
- **Concurrency Walkthrough**: Describe the write-mediation contract, conflict model, and a representative multi-plugin write scenario to validate that the design is implementable.
tokens used
51,793
2026-04-06T06:47:40.060353Z ERROR rmcp::transport::streamable_http_client: fail to delete session: Client error: HTTP status client error (404 Not Found) for url (https://mcp.stripe.com/) session_id="55a06245-57c6-4458-adb9-d800c5a8010a"
